server:
  port: 8087
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lr3881
    driverClassName: org.postgresql.Driver
    username: product
    password: product
    hibernate:
      ddl-auto: create-drop
